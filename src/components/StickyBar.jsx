import { useEffect, useState } from 'react'
export default function StickyBar(){const [show,setShow]=useState(false);useEffect(()=>{const check=()=>setShow(scrollY>window.innerHeight*.8);addEventListener('scroll',check,{passive:true});check();return()=>removeEventListener('scroll',check)},[]);if(!show)return null;return <button onClick={()=>document.querySelector('#decision')?.scrollIntoView({behavior:'smooth'})} className="fixed bottom-3 left-3 right-3 z-30 min-h-12 rounded-xl bg-[#1B2A49] px-4 text-sm font-bold text-[#FBF6EC] shadow-xl sm:hidden">Decision pending — tap to respond →</button>}
